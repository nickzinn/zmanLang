// Arrays: allocation, indexing, and length()

// Unspecified elements keep the zero-initialized default.
let a := {10,20,0,0,7};

println(text(a[0]) + "," + text(a[1]) + "," + text(a[4]));
println(text(length(a)));

let s := "hi\n";
println(text(length(s)));
